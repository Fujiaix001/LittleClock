# LittleClock 4.0.26

## 保守清理

- 移除 1 個未使用欄位、3 個未使用方法與 3 個未使用 import。
- 天氣請求成功後只更新一次快取畫面，避免重複排版與位置恢復。
- 番茄鐘執行中由共用 ticker 統一排程，避免每秒重複取消與建立 callback。
- 延伸天氣進度線隱藏時不再執行無效的時間更新與 `invalidate()`。

## 相容性

- 不更改功能、畫面設定、天氣快取格式或 SharedPreferences key。
- 保留舊版排版、低耗電模式與 Android 4.2 相容程式。
- 不新增第三方依賴、背景服務或執行緒。
