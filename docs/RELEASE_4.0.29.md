# LittleClock 4.0.29

## 手動更新動畫

- 長按並向下拉動成功後，更新提示會以短暫的下落縮放回彈動畫出現。
- 冷卻時間內重複操作，或未啟用天氣時，仍會顯示同樣的手勢成功回饋。
- 天氣請求完成後繼續顯示成功或失敗文字，不改變原有快取保留邏輯。

## 效能與相容性

- 動畫只在手勢觸發時執行一次，約 320ms。
- 只變更 `alpha`、`scale` 與 `translation` 繪圖屬性，不觸發重新排版，也不新增常駐服務、執行緒或第三方依賴。
- 保留 Android 4.2（API 17）相容性。
