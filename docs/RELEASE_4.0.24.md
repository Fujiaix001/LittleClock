# LittleClock 4.0.24

## 獨立延伸天氣資訊

- 新增「顯示延伸天氣資訊」獨立開關，預設關閉，維持原本精簡使用體驗。
- 開啟後顯示未來三小時的時間、溫度與降雨機率。
- 顯示日出、日落與目前日照進度線。
- 延伸資料沿用既有天氣更新週期與背景執行緒，不新增常駐服務或額外輪詢。
- 延伸資料請求失敗時會保留快取，並回退到原本的目前天氣請求。

## 相容性

- 「精簡排列（無地名時與日期同列）」設定與既有三種字體的排版邏輯不變。
- 延伸資訊關閉時不建立可見內容、不增加網路欄位，也不改變原有天氣顯示。
